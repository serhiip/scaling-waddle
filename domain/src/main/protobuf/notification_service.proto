syntax = "proto3";

import "notification.proto";
option java_package = "car.drivernotifier.protos";

package notification;

service NotificationService {  
  rpc count (GetCount) returns (Count) {};
}
